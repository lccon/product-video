<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cilicili.jz2.dao.CommentMapper">
  <resultMap id="BaseResultMap" type="cilicili.jz2.domain.Comment">
    <id property="id" column="id"/>
    <result property="videoId" column="video_id"/>
    <result property="userId" column="user_id"/>
    <result property="content" column="content"/>
    <result property="sendtime" column="sendtime"/>
    <result property="countLike" column="count_like"/>
  </resultMap>

  <sql id="Base_Column_List">
    id, video_id, user_id, content, sendtime, count_like
  </sql>

  <insert id="insertComment" parameterType="cilicili.jz2.domain.Comment">
    insert into comments
    (id, video_id, user_id, content, sendtime, count_like)
    values
    (#{id}, #{videoId}, #{userId}, #{content}, #{sendtime}, #{countLike})
  </insert>

  <update id="updateByPrimaryKeySelective" parameterType="cilicili.jz2.domain.Comment">
    update comments
    <set>
      <if test="videoId != null">
        video_id = #{videoId},
      </if>
      <if test="userId != null">
        user_id = #{userId},
      </if>
      <if test="content != null">
        content = #{content},
      </if>
      <if test="sendtime != null">
        sendtime = #{sendtime},
      </if>
      <if test="countLike != null">
        count_like = #{countLike},
      </if>
    </set>
    where id = #{id}
  </update>

  <delete id="deleteComment" parameterType="java.lang.Integer">
    delete from comments where id = #{id}
  </delete>

</mapper>